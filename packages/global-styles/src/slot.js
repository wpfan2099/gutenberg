/**
 * WordPress dependencies
 */
import { createSlotFill } from '@wordpress/components';

export const GlobalStylesSlot = createSlotFill( '__GLOBAL_STYLES_SLOT__' );
export const { Slot, Fill } = GlobalStylesSlot;
